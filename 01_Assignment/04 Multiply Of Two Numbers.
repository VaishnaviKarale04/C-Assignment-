#include<stdio.h>
#include<conio.h>

int main()
    {
        int Num1 = 0, Num2 = 0, Mult = 0;

        printf("\n Enter a First Number : ");
        scanf("%d",&Num1);

        printf("\n Enter a Second Number : ");
        scanf("%d",&Num2);

        Mult = Num1 * Num2;

        printf("\n Multiply Of Two Numbers : %d ", Mult);

        getch();
        return 0;
    }
